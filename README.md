# 📝 Blog It!

Generic blog website I built to learn the fundamentals of Ruby on Rails

## 👨🏻‍💻 Development

```bash
# install gem dependencies
bundle install

# install yarn dependencies
yarn install

# run migrations
rails db:migrate

# start rails server
rails s
```

This will serve the files at `http://localhost:3000/`